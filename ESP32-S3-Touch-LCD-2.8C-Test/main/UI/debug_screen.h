#pragma once

// ── Debug screen — shows live OBD values for in-car testing ──────
// Replaces the demo. Shows RPM, Water Temp, Oil Temp.
// Updates automatically via LVGL timer.

void debug_screen_create(void);
